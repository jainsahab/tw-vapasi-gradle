apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDirs = ['src/main/java']
        }
    }
}

jar {
    baseName = 'greet'
    manifest {
        attributes 'Main-Class': 'Greet'
    }
}
