task upper {
    doLast {
        String someString = 'Vapasi by ThoughtWorks !!'
        println "Original: $someString"
        println "Upper case: ${someString.toUpperCase()}"
    }
}

task count {
    doLast {
        5.times { print "Number : ${it}\n"}
    }
}